import { videoOptionsSchema } from '@cloudinary-util/url-loader/dist/schema';
import { zodToJsonSchema } from 'zod-to-json-schema';

import SchemaTable from '../../components/SchemaTable';


# VideoOptions

The VideoOptions type is an extension of the Asset type, adding Video-specific properties.

## Properties

<SchemaTable schema={zodToJsonSchema(videoOptionsSchema, 'videoOptions')} schemaKey="videoOptions" />