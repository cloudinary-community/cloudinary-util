import { imageOptionsSchema } from "@cloudinary-util/url-loader/schema";
import { zodToJsonSchema } from "zod-to-json-schema";

import SchemaTable from "@/components/SchemaTable";

# ImageOptions

The ImageOptions type is an extension of the [AssetOptions](/assetoptions) type, adding Image-specific properties.

## Properties

<SchemaTable
  schema={zodToJsonSchema(imageOptionsSchema, "imageOptions")}
  schemaKey="imageOptions"
/>
